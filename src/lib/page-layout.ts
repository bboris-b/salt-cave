/**
 * Gabbia orizzontale allineata a Prezzi / Prenotazione (max 1100px, padding coerente).
 * Griglia a 12 colonne: usare `lg:grid-cols-12` + `lg:col-span-*` sul contenitore interno.
 */
export const SITE_GRID_WRAP = 'mx-auto w-full max-w-[1100px] px-5 lg:px-8'

/** Gap tipico tra colonne nella stessa riga (calendario / form usa lg:gap-12). */
export const SITE_GRID_GAP = 'gap-10 lg:gap-12'
